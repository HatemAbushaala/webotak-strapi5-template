{
  "collectionName": "components_pages_cta",
  "info": {
    "displayName": "CTA",
    "description": ""
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "description": {
      "type": "text"
    },

    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "alignment": {
      "type": "enumeration",
      "enum": ["left", "center", "right"],
      "default": "center"
    },
    "variation": {
      "type": "enumeration",
      "enum": ["small", "medium", "two-columns"],
      "default": "small"
    }
  }
}
